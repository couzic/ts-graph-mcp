/** Edge types traversed by dependency tools */
export const EDGE_TYPES = [
  "CALLS",
  "INCLUDES",
  "REFERENCES",
  "EXTENDS",
  "IMPLEMENTS",
];

/** Maximum traversal depth for recursive queries */
export const MAX_DEPTH = 100;
