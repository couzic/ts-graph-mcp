{
  "name": "ts-graph-mcp",
  "version": "0.3.3",
  "description": "TypeScript code graph with MCP server and Web UI for AI coding agents",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "ts-graph-mcp": "dist/main.js"
  },
  "workspaces": [
    "http",
    "mcp",
    "shared",
    "ui"
  ],
  "scripts": {
    "prepublishOnly": "npm i && npm run check",
    "check": "npm test && npm run test:e2e && npm run build && npm run lint:fix && npm run check:ui",
    "check:ui": "cd ui && npm run typecheck",
    "start": "node dist/main.js",
    "start:mcp": "node dist/main.js --mcp",
    "prebuild": "rm -rf dist",
    "build": "tsc && tsc-alias && npm run build:ui && chmod +x dist/main.js && cp package.json dist",
    "build:ui": "cd ui && npm run build",
    "test": "vitest run",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "test:watch": "vitest",
    "dev": "tsc --watch",
    "dev:ui": "cd ui && npm run dev",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "benchmark:call-chain": "npm run build && npx tsx benchmark/lib/setup.ts sample-projects/call-chain && npx tsx benchmark/lib/run.ts sample-projects/call-chain",
    "benchmark:layered-api": "npm run build && npx tsx benchmark/lib/setup.ts sample-projects/layered-api && npx tsx benchmark/lib/run.ts sample-projects/layered-api",
    "benchmark:monorepo": "npm run build && npx tsx benchmark/lib/setup.ts sample-projects/monorepo && npx tsx benchmark/lib/run.ts sample-projects/monorepo",
    "benchmark:clean-architecture": "npm run build && npx tsx benchmark/lib/setup.ts sample-projects/clean-architecture && npx tsx benchmark/lib/run.ts sample-projects/clean-architecture",
    "benchmark:all": "npm run benchmark:call-chain && npm run benchmark:clean-architecture && npm run benchmark:layered-api && npm run benchmark:mixed-types && npm run benchmark:monorepo"
  },
  "keywords": [
    "typescript",
    "code-graph",
    "mcp",
    "static-analysis"
  ],
  "license": "MIT",
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "dist/public"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@orama/orama": "^3.1.18",
    "better-sqlite3": "^11.0.0",
    "chalk": "^5.0.0",
    "chokidar": "^5.0.0",
    "express": "^5.0.0",
    "msgpackr": "^1.11.8",
    "node-llama-cpp": "^3.15.1",
    "rxjs": "^7.0.0",
    "ts-morph": "^24.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/better-sqlite3": "^7.0.0",
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.0.0",
    "vitest": "^4.0.16"
  },
  "optionalDependencies": {
    "neo4j-driver": "^5.0.0"
  }
}
